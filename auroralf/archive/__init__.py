"""Historical AuroraLF implementations excluded from production APIs.

Modules in this namespace are retained for exact diagnostic reproduction.
Importing them is an explicit archival opt-in; they must not be used as
production model components without a separate validation and promotion step.
"""
